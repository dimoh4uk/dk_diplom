mixin activityMain(activity)
    .hero
        img(src=activity.photoLink width='1344' height='564')
        .hero-body
            .main-slider__wrap
                .title.is-2= activity.name
                a(href=activity.link).button Подробнее