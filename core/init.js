const RoleStaffModel = require('../models/role-staff.model');

module.exports.initBaseRole = async () => {
    // console.log("init manager");
    // return RoleStaffModel.create({name: 'manager'});
}